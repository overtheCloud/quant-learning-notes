# 量化基础（重编版）—— 面向 freqtrade 的零基础入门

> 来源：聚宽《量化交易零基础入门教程》（11 章）的通用知识提炼。
> 已剔除全部聚宽平台操作（注册、网页界面、聚宽 API、notebook 等），保留放之四海皆准的量化骨架，并把每个概念映射到 freqtrade。
> 教程原意不变：策略框架、回测含义、止损、未来函数、过拟合——这些在 freqtrade 里会原样遇到。

---

## 0. 一张对照表：聚宽概念 → freqtrade 对应物

先建立映射关系，后面所有内容都按右列理解：

| 通用概念 | 聚宽里的样子（已剔除） | freqtrade 里的样子 |
|---|---|---|
| 策略框架（初始化+周期循环） | `initialize` + `handle_data` | `IStrategy` 类：`bot_loop_start` / `populate_indicators` + `populate_entry_trend` + `populate_exit_trend` |
| 交易标的 | 股票代码 `'000001.XSHE'` | 交易对 `"BTC/USDT"`，在配置文件 `pair_whitelist` 里定义 |
| 行情数据（OHLCV） | `get_price` / `history` | `dataframe`（每行一根 K 线：date/open/high/low/close/volume），由 `download-data` 下载 |
| 回测 | 网页上点"编译运行" | `freqtrade backtesting --strategy Xxx --timerange ...` 命令行 |
| 模拟交易 | 聚宽"模拟交易"功能 | `freqtrade trade --dry-run`（干跑模式，用真实行情不下真单） |
| 实盘 | 聚宽对接券商 | `freqtrade trade`（对接交易所 API，真金白银） |
| 下单函数 | `order` / `order_value` 等 | 你不用写下单代码——策略只输出"买/卖信号"，下单由框架执行 |
| 止损 | 自己在循环里写判断清仓 | 配置 `stoploss = -0.10`，或写 `custom_stoploss()` |
| 全局变量 | `g.xxx` | 策略类属性 / `self.xxx` |
| 基准收益 | 沪深300 | 通常对比"持币不动"或某个币的同期涨跌 |
| 研究环境 | 聚宽 notebook | 本地 Jupyter / 直接写脚本分析下载的数据 |

**核心差异（也是 freqtrade 的优势）**：聚宽教程里大量篇幅在教"怎么在聚宽网页上点按钮、怎么调聚宽 API"；freqtrade 里这些都不存在——你在本地写一个 Python 策略类，命令行跑回测，一切都在自己机器上。

---

## 1. 初识量化交易

### 1.1 什么是量化交易

借助统计和数学方法，用计算机来执行交易。本质就三步：

1. **获取数据**：读取价格、成交量等行情。
2. **制定规则**：明确什么条件买、什么条件卖。
3. **自动执行/提示**：程序持续监控，条件满足就行动。

量化的价值：
- **效率**：一次回测几分钟，等于人工验证几百个交易日；
- **客观**：用年化收益、最大回撤、夏普比率等统计指标评价，而不是"感觉不错"；
- **覆盖**：计算机可同时盯几十个交易对，人盯不过来；
- **纪律**：速度快、执行统一、不会犹豫，没有情绪化操作。

### 1.2 量化方法论：灵感 → 细化 → 程序 → 检验（全教程最重要的一节）

**第一步：从一个灵感开始。** 灵感就是你想验证的可能盈利的方法，比如"价格跌到均线下方后会反弹"。来源：阅读、交流、自己观察。

**第二步：把灵感细化成明确、可执行的策略。** 灵感通常是模糊的，必须细化到"什么条件、什么品种、多少量"，标准是：**让任何人按你的描述在相同情形下做出相同操作**。

教程示例（逐步消除模糊性）：
- 模糊版："股价显著低于近几日平均价就买"——多低算显著？几日？买多少？
- 细化一："股价低于近 20 日均价 10% 则全仓买入；高于 10% 则清仓"
- 细化二：补上标的范围（监测哪些品种）、排除高风险标的

**第三步：把策略写成程序。** 用代码把规则翻译给计算机。

**第四步：检验效果，两种基本方法：**
- **回测（Backtest）**：用**历史数据**模拟执行策略。设定初始资金和时间区间，把历史行情喂给程序，输出盈亏报告。结果不好→改进；结果好→进入下一步。
- **模拟交易（Paper Trading）**：用**未来的实时行情**模拟执行。回测是看过去，模拟是看未来。

> freqtrade 对应：回测 = `freqtrade backtesting`；模拟 = `--dry-run`。先回测、再 dry-run、最后才考虑实盘，这个顺序不能跳。

### 1.3 做量化需要什么

- **数据**：行情的获取、存储、清洗。freqtrade 用 `freqtrade download-data` 从交易所拉历史数据。
- **系统**：能写策略、跑回测、执行交易的系统。freqtrade 本身就是这样一个完整系统（这也是选它的原因——不用自己造轮子）。

---

## 2. 策略的基本框架：初始化 + 周期循环

几乎所有量化策略都长一个样：

- **初始化**：运行前的一次性准备——选定交易品种、设置参数。
- **周期循环**：每个周期（每根 K 线/每天）重复做的事——算指标、判断买卖。

类比：人做交易也符合这个框架——初始化 = 你脑子里已有的交易思想；周期循环 = 每天看盘、判断、下单的动作。

教程贯穿示例："每天买 100 股平安银行" → 框架化：

```
初始化：选定要交易的股票为平安银行
每天循环：买 100 股
```

**回测三要素**：初始资金、起止时间、频率（天/分钟）。回测结果看收益曲线与基准对比。

**freqtrade 里的对应结构**（后面第 12 节有练习）：
- 初始化 = 策略类的属性配置（`timeframe`、`stoploss`、`pair_whitelist`）
- 周期循环 = 每根 K 线收盘后框架调用你的 `populate_indicators` → `populate_entry_trend` → `populate_exit_trend`

**debug 入门（通用编程素养）**：报错日志最后一行是错误类型（如 `SyntaxError`），前面是出错位置。新手三大错误：① 用了中文标点；② 拼写错误；③ 缩进不对。排错（debug）花的时间通常不低于写代码，这是正常且必须掌握的能力。

---

## 3. Python 基础速查（只列量化用得上的）

教程用量化场景教 Python，这里浓缩成速查。快速过一遍即可。

- **变量与赋值**：`a = 1`，变量是有名字的"盒子"，赋值即替换。
- **数据类型**：数字（int/float/bool）、字符串（交易对名 `"BTC/USDT"` 就是字符串）、列表、字典。
- **列表（list）**：存一组数据。`pairs = ["BTC/USDT", "ETH/USDT"]`；索引从 0 开始；切片 `list[1:3]`。
- **字典（dict）**：自定义索引。`{"open": 10.0, "close": 11.0}`；`dict.keys()` / `dict.values()`。
- **条件判断**：`if / elif / else`，可嵌套；运算符 `== != > < >= <= and or not`。
- **for 循环**：遍历标的池的核心工具。`for pair in pairs: ...`
  - `break`：跳出整个循环；`continue`：跳过本次、继续下一只（"跳过不符合条件的币"用 continue）。
- **自定义函数**：`def 函数名(参数):` + `return`。函数内数据通过参数进、返回值出。
- **注释**：`#` 开头，强烈建议写。
- **print / log**：调试核心手段。freqtrade 里用 `logger.info(...)`。

**pandas DataFrame（freqtrade 的数据核心）**：二维表，行=K 线（按时间），列=字段。你的策略里 `dataframe` 就是它。
- 取列：`dataframe['close']`；
- 滚动计算：`dataframe['close'].rolling(20).mean()`（20 日均线就是这么算的）；
- 条件列：`dataframe.loc[dataframe['close'] < dataframe['ma20'], 'enter_long'] = 1`——这就是"把买卖规则写成列"。

---

## 4. 下单语义与订单类型

教程讲了四种通用下单语义（freqtrade 里你基本不用手写，但必须懂）：

1. **按数量买卖**：买/卖指定数量；
2. **调仓到目标数量**：高于目标卖、低于目标买；
3. **按金额买卖**：买/卖指定金额（系统按现价折算数量）；
4. **调仓到目标市值**。

订单类型：
- **市价单**：按当前价立即成交（freqtrade 默认）；
- **限价单**：指定价格，到了才成交。

**回测系统的仿真**：真实回测引擎会自动处理手续费、滑点（下单对成交价的不利冲击）、撮合规则。freqtrade 回测同样内置这些，配置里可调手续费率。

> freqtrade 特色：你只在策略里输出信号（`enter_long=1` / `exit_long=1`），仓位大小由配置决定（`stake_amount`），下单、撤单、超时处理全部框架代劳。这比聚宽"自己调下单函数"省心很多。

---

## 5. 运行时状态与止损

### 5.1 运行时状态（context 概念）

任何回测/交易系统都有一个运行时上下文，存着：**当前时间、持仓、持仓成本、可用资金、累计收益**。策略逻辑靠读这些数据做决策。

常用状态类别（通用）：当前时间、可用资金、持仓市值、某持仓的开仓成本、某持仓的现价与可卖数量。

> freqtrade 对应：回测中这些由框架管理；`custom_stoploss` / `custom_exit` 等钩子函数会传入 `trade` 对象（含开仓价、当前利润等）和 `current_time`。

### 5.2 止损（必须掌握）

- **定义**：亏损达到一定幅度后卖出，目的是**截断进一步的亏损**。
- **实现原理（通用逻辑）**：
  1. 取持仓成本和现价；
  2. 算收益率 = 现价/成本 − 1；
  3. 若收益率 < 阈值（如 −10%），清仓。
- **止盈**是对称概念：盈利达到阈值后落袋。

> freqtrade 对应：最简形式一行配置 `stoploss = -0.10`（跌 10% 自动卖）。进阶用 `custom_stoploss()` 写移动止损。**止损是新手策略里最容易漏掉的部分，别漏。**

---

## 6. 多标的策略：标的池 + 遍历

单标的逻辑重复写 N 遍是坏味道；通用解法：**用列表存标的池，for 循环遍历执行**。绝大多数多标的策略（轮动、选股、再平衡）都是这个结构。

示例逻辑（教程的多股票+逐股止损）：

```
对池中每只标的：
    收益率 = 现价/成本 - 1
    若 收益率 < -10%：清仓该标的
```

要点：**止损是逐标的计算、逐标的触发的**。

> freqtrade 对应：`pair_whitelist` 就是标的池；框架自动对每个交易对跑你的策略逻辑，你不需要自己写遍历。多标的轮动通过 `max_open_trades` + 排序打分实现（进阶内容）。

---

## 7. 数据：行情、财务、可见性

### 7.1 行情数据（OHLCV）

每根 K 线的通用字段：open（开）、high（高）、low（低）、close（收）、volume（量），外加成交额、均价、前收等。**所有技术指标都是从这几个数字算出来的。**

- **复权**：股票因分红拆股需要复权处理；**币圈没有这个问题**，这是从 A 股教程迁移过来可以少学的一块。
- 两种取数模式：多标的×单字段、单标的×多字段。

> freqtrade 对应：`freqtrade download-data --exchange binance --pairs BTC/USDT --timeframe 1h --days 90`，数据存本地，回测直接读。

### 7.2 财务数据（币圈基本不适用，了解即可）

教程讲了财报周期、单季度 vs 报告期口径等。**加密市场没有财报**，这一节对 freqtrade 用户价值不大，跳过不亏。唯一要保留的概念是下一节的"数据可见性"。

### 7.3 数据可见性（point-in-time）——回测数据设计的第一原则

回测是模拟"当时"的市场。策略在 T 时刻只能使用 T 时刻**已经存在**的数据。任何违反这一点的代码都会让回测严重失真（即未来函数，见第 9 节）。

---

## 8. 综合实战：从灵感到完整策略（小市值轮动案例）

教程用一个完整案例演示全流程，这里保留方法论，换成币圈语境。

### 8.1 灵感细化

灵感："小市值的币过去一段时间表现好"。细化成文字规则：
1. 每天选出市值最小的前 N 个币；
2. 已持有但已不在选股列表中的，卖出；
3. 用可用资金的 N 分之一买入每个新币。

**关键一步：参数化。** "10 个"不一定是唯一选择，把 N 设为可调参数。**把魔法数字变成可调参数是策略细化的关键动作。**

### 8.2 逐步实现

- 先搭"初始化+周期循环"骨架，把每条规则写成注释占位；
- **把复杂拆成简单，逐步实现；不确定的地方用打印检查。**

### 8.3 调整改进：控制交易频率

发现每天调仓太频繁 → 引入调仓周期 `period`，实现"每 period 天调仓一次"。通用实现：计数器 + 取余判断（`天数 % period == 0`）。

教程回测观察：该策略 2015–2016 表现好、2017 后平平——**恰好呼应灵感本身（"最近不太行了"）**。这就是回测的价值：验证或证伪灵感。也说明**策略有时效性**。

### 8.4 延伸思考（教程原话，值得做）

- 参数设为多少效果好？（→ 参数优化，freqtrade 用 `hyperopt`）
- 等额买入后为何各持仓价值越差越大？（涨跌不同 → 引出再平衡）
- 加止损/止盈；
- 选股时排除高风险/不可交易标的（A 股排除 ST/停牌/涨停；币圈对应排除流动性差、刚上线、即将退市的币）。

> freqtrade 对应：这个"因子打分 + 定期轮动 + 等权配置"范式可以直接落地——`pair_whitelist` 放候选池，策略里按因子排序，配合 `max_open_trades` 控制持仓数。

---

## 9. 策略评价：指标、陷阱与失效（全教程含金量最高的一章）

### 9.1 回测评价指标

| 指标 | 含义 | 怎么看 |
|---|---|---|
| 策略收益 | 回测期间总收益率 | 与回测时长强相关，不利于跨策略比较 |
| 基准收益 | 市场整体的同期收益 | **策略长期跑不赢基准 ≈ 失败**（不如直接拿住 BTC） |
| 年化收益率 | 折算成一年的收益率 | 统一了时间标准，越大越好 |
| 最大回撤 | 净值从峰值到谷底的最大跌幅 | 衡量风险，越小越好。直觉：你现在跟单，最坏情况就是一上来就撞上历史上跌得最惨的那一段 |
| 交易次数 | 样本量 | **次数太少则结果不可信**——10 年年化惊人但只交易 2 次，没人敢跟 |
| Alpha / Beta | 独立于市场的收益 / 跟随市场的倍率 | Alpha 越大越好；年化虽负但 Alpha 为正，说明策略有超额能力，被市场部分拖累 |
| 夏普比率 | 每承担一单位风险换来多少收益 | 风险调整后收益，越大越好 |

> freqtrade 对应：`freqtrade backtesting` 报表自带总收益、胜率、平均盈亏、回撤、夏普等；`--breakdown day/week/month` 还能看分时段表现。

### 9.2 未来函数（重点）

- **定义**：策略用了"当时"不可能知道的信息，回测结果严重失真。
- 典型例子："每天买明天涨停的股票"——回测里"明天"是已知的，能跑出完美曲线，现实中毫无价值。
- **排查方法**：① 人工审查代码中一切与时间有关的地方（尤其注意数据接口的时间默认处理）；② 跑几天模拟交易——模拟交易拿不到未来数据，有未来函数的策略往往模拟表现与回测天差地别。
- 隐蔽情形：同一份代码模拟正常、回测却偷看了未来——回测漂亮但失真，模拟跑久了会与回测相去甚远。
- **经验法则**：回测收益极高、回撤极低、各时段都完美，感觉自己发现印钞机时——**大概率有未来函数**。

### 9.3 过拟合（重点）

- **定义**：挑参数时专挑回测最好的那组，换一段数据或上实盘就失效。参数越多越容易过拟合。
- **经典比喻**：老师不教原理，只让学生背试卷答案——同一张卷子满分，换卷子就完蛋。
- **应对**：
  - 关注参数**鲁棒性**（参数小幅变化，结果不该剧变）；
  - 控制参数数量；
  - 多测几组参数，观察参数变化对结果的影响；
  - **样本外测试**：用一段数据挑参数，用另一段数据验证。
- > freqtrade 对应：`hyperopt` 是参数优化器，但必须配合样本外验证使用，否则只是"更高级的背答案"。

### 9.4 策略失效

策略有时效性，没有永远的印钞机。警示信号：回撤创历史新高、收益曲线停滞。四类常见原因：
1. **逻辑基础不再成立**（制度/市场结构变了）——所以必须理解策略**为什么**有效；
2. **资金量过大**：冲击成本吞噬利润，策略有容量上限，资金应逐步加大；
3. **同类策略太多**：大家抢同一份钱，表现为想买的价格买不到；
4. **被针对**：别人监测到你的行为模式并寄生其上（先你一步买入、等你拉升后卖出）。

### 9.5 收益与风险的取舍

盈利能力和抗风险往往不可兼得。**建议：达到基本收益能力后，极力追求低风险。** 理由：收益可以靠加资金放大，风险不能。例：策略 A 年化 300% 回撤 50%，策略 B 年化 30% 回撤 5%——给 B 十倍资金，盈利相同，但 A 永远学不会 B 的稳。

### 9.6 模拟交易：回测与实盘之间的必经环节

回测（历史）→ 模拟交易（未来实时行情，不投真钱）→ 实盘。模拟能发现回测发现不了的问题（未来函数、撮合差异、延迟）。

> freqtrade 对应：`--dry-run` 模式就是模拟交易，跑一段时间、对比 dry-run 与回测的一致性，再考虑小资金实盘。

---

## 10. 研究环节

策略开发前通常先做研究：拿数据探索规律、画图验证想法。教程案例：分析同行业两只股票市值比率的波动区间 → 发现比率围绕均值波动 → 构造布林带信号（突破上轨说明前者相对高估，切换持有后者；跌破下轨反向；回中轨各半仓）→ 变成均值回归/配对交易策略。

这就是完整链条：**观察统计规律 → 提出假设 → 写成策略 → 回测验证**。

> freqtrade 对应：`download-data` 拉数据 → Jupyter/脚本里用 pandas + matplotlib 分析 → 想法成立再写进策略。

---

## 11. 成长指路

- **自学是第一能力**：量化是不适合分享的行业，有价值的东西不会现成摆在那，靠"大浪淘金"式地寻找、思索。
- **进阶四个经典方向**（教程推荐）：双均线策略（趋势跟踪）、彼得·林奇的成功投资（基本面选股）、Fama-French 三因子（因子投资）、凯利公式（仓位管理）。
- **成长模式**：有灵感 → 研究实现 → 遇到新知识缺口就补 → 成败都有新认识 → 下一个灵感，循环前进。
- **灵感来源**：论文、研报、社区、书籍，以及跨领域视角（统计、机器学习、行为金融、心理学）；**直接参与市场**本身就是重要灵感来源。保持批判与独立思考，警惕盲从。
- **警惕照搬**：别人公开的策略直接搬来大概率不赚钱（见 9.4 第 3 条）。

---

## 12. 学完之后：在 freqtrade 里把概念落地（练习清单）

按顺序做，每个练习对应上面的概念：

1. **环境**：装好 freqtrade，`freqtrade download-data` 下载 BTC/USDT 1h 数据（对应 §7）
2. **第一个策略**：复制官方模板 `SampleStrategy`，读懂 `populate_indicators/entry/exit` 三段结构（对应 §2 框架）
3. **均线策略**：写"收盘上穿 20 日均线买入、下穿卖出"（对应 §1.2 灵感细化 + §3 pandas）
4. **跑回测**：`freqtrade backtesting`，读懂报表里的收益/回撤/胜率/交易次数（对应 §9.1）
5. **加止损**：配置 `stoploss`，对比加前加后的最大回撤（对应 §5.2）
6. **dry-run**：`freqtrade trade --dry-run` 跑几天，观察信号与回测的差异（对应 §9.6）
7. **参数实验**：手动改均线周期（10/20/50）对比结果，体会过拟合与鲁棒性（对应 §9.3）

做完 1–7，聚宽教程的全部通用概念你就都在 freqtrade 里亲手用过了。之后进入学习路线第 2 步（freqtrade 官方文档）。
